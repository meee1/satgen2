<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Racelogic.Gnss.SatGen.BlackBox.RealTimeView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:si="clr-namespace:Expression.Samples.Interactivity;assembly=Expression.Samples.Interactivity" xmlns:im="clr-namespace:Microsoft.Expression.Interactivity.Media;assembly=Microsoft.Expression.Interactions" xmlns:local="clr-namespace:Racelogic.Gnss.SatGen.BlackBox" xmlns:satgen="clr-namespace:Racelogic.Gnss.SatGen;assembly=Racelogic.Gnss.SatGen" xmlns:ctr="clr-namespace:Racelogic.WPF.Controls;assembly=Racelogic.WPF.Controls" xmlns:utils="clr-namespace:Racelogic.WPF.Utilities;assembly=Racelogic.WPF.Utilities" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" x:Name="root" MinWidth="646" MinHeight="332" UseLayoutRounding="True" SnapsToDevicePixels="True" ResxExtension.DefaultResxName="Racelogic.SatGen.Properties.Resources" Language="{UICulture}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <local:AttenuationMultiValueConverter x:Key="AttenuationMultiValueConverter" />
      <utils:NegatingConverter x:Key="NegatingConverter" />
      <utils:EnumToBoolConverter x:Key="EnumToBoolConverter" />
      <utils:ValueConverterGroup x:Key="EnumToVisibilityConverter">
        <utils:EnumToBoolConverter />
        <utils:BoolToVisibilityConverter />
      </utils:ValueConverterGroup>
      <utils:DoubleToBoolConverter x:Key="DoubleToBoolConverter" />
      <utils:ValueConverterGroup x:Key="DoubleToVisibilityConverter">
        <utils:DoubleToBoolConverter />
        <utils:BoolToVisibilityConverter />
      </utils:ValueConverterGroup>
      <utils:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
      <Style x:Key="{x:Type RadioButton}" TargetType="{x:Type RadioButton}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
        <Setter Property="ContentTemplate">
          <Setter.Value>
            <DataTemplate>
              <Label Padding="0" Margin="0" Content="{Binding}" />
            </DataTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Storyboard x:Key="ShowStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.25" To="0.0" Duration="0:0:15" AccelerationRatio="0.3" Storyboard.Target="{Binding RelativeSource={RelativeSource AncestorType=Grid}}" />
      </Storyboard>
      <Storyboard x:Key="HideStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.20" To="0.0" Duration="0:0:15" AccelerationRatio="0.3" Storyboard.Target="{Binding RelativeSource={RelativeSource AncestorType=Grid}}" />
      </Storyboard>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Margin="-2,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="100" />
      <RowDefinition Height="8" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid Row="0" Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <GroupBox Name="StatusGroupBox" Header="Status" Grid.Row="0" Margin="8, 0, 3, 6">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBlock Text="Synchronising" Margin="0" Grid.Row="0" Visibility="Hidden" />
          <ctr:LedGauge Name="StatusLed" Grid.Row="1" IsOn="True" Width="52" Height="52" HorizontalAlignment="Center" Margin="0,1,0,-1">
            <FrameworkElement.Style>
              <Style TargetType="{x:Type ctr:LedGauge}">
                <Style.Triggers>
                  <DataTrigger Value="None" Binding="{Binding ConnectionStatus}">
                    <Setter Property="ctr:LedGauge.Color" Value="Gray" />
                  </DataTrigger>
                  <DataTrigger Value="Connected" Binding="{Binding ConnectionStatus}">
                    <Setter Property="ctr:LedGauge.Color" Value="Yellow" />
                  </DataTrigger>
                  <DataTrigger Value="Transmitting" Binding="{Binding ConnectionStatus}">
                    <Setter Property="ctr:LedGauge.Color" Value="Green" />
                  </DataTrigger>
                  <DataTrigger Value="BufferUnderrun" Binding="{Binding ConnectionStatus}">
                    <Setter Property="ctr:LedGauge.Color" Value="Red" />
                  </DataTrigger>
                  <DataTrigger Value="ConnectionLost" Binding="{Binding ConnectionStatus}">
                    <Setter Property="ctr:LedGauge.Color" Value="Red" />
                    <Setter Property="ctr:LedGauge.IsFlashing" Value="True" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
          </ctr:LedGauge>
          <TextBlock Name="BufferUnderrunCountTextBlock" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFFFF" FontWeight="Bold" Text="{Binding BufferUnderrunCount}" Visibility="{Binding BufferUnderrunCount, Converter={StaticResource DoubleToVisibilityConverter}}">
            <FrameworkElement.Style>
              <Style TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="28" />
                <Setter Property="Margin" Value="0,-3,-2,0" />
                <Style.Triggers>
                  <DataTrigger Value="True" Binding="{Binding BufferUnderrunCount, Converter={StaticResource DoubleToBoolConverter}, ConverterParameter=9}">
                    <Setter Property="FontSize" Value="26" />
                    <Setter Property="Margin" Value="0,-3,-1,0" />
                  </DataTrigger>
                  <DataTrigger Value="True" Binding="{Binding BufferUnderrunCount, Converter={StaticResource DoubleToBoolConverter}, ConverterParameter=99}">
                    <Setter Property="FontSize" Value="20" />
                    <Setter Property="Margin" Value="0,-2,-1,0" />
                  </DataTrigger>
                  <DataTrigger Value="True" Binding="{Binding BufferUnderrunCount, Converter={StaticResource DoubleToBoolConverter}, ConverterParameter=999}">
                    <Setter Property="FontSize" Value="15" />
                    <Setter Property="Margin" Value="0,-1,-1,0" />
                  </DataTrigger>
                  <DataTrigger Value="True" Binding="{Binding BufferUnderrunCount, Converter={StaticResource DoubleToBoolConverter}, ConverterParameter=9999}">
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="Margin" Value="0,0,0,0" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
          </TextBlock>
          <TextBlock Grid.Row="3" HorizontalAlignment="Center" Margin="0" Padding="0">
            <FrameworkElement.Style>
              <Style TargetType="{x:Type TextBlock}">
                <Style.Triggers>
                  <DataTrigger Value="None" Binding="{Binding ConnectionStatus}">
                    <Setter Property="Text" Value="Disconnected" />
                  </DataTrigger>
                  <DataTrigger Value="Connected" Binding="{Binding ConnectionStatus}">
                    <Setter Property="Text" Value="Synchronising" />
                  </DataTrigger>
                  <DataTrigger Value="Transmitting" Binding="{Binding ConnectionStatus}">
                    <Setter Property="Text" Value="Transmitting" />
                  </DataTrigger>
                  <DataTrigger Value="BufferUnderrun" Binding="{Binding ConnectionStatus}">
                    <Setter Property="Text" Value="Overload!" />
                  </DataTrigger>
                  <DataTrigger Value="ConnectionLost" Binding="{Binding ConnectionStatus}">
                    <Setter Property="Text" Value="Connection lost!" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
          </TextBlock>
        </Grid>
      </GroupBox>
      <GroupBox Name="SatLimitGroupBox" Header="Satellite count limit" Grid.Column="1" Margin="3,1,0,6">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <ComboBox Name="SatCountLimitComboBox" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0" MinWidth="{Binding ElementName=SatCountLimitComboBoxGhost, Path=ActualWidth}" ItemsSource="{Binding AvailableSatCountLimitModes}" SelectedItem="{Binding SatCountLimitMode}" />
          <ComboBox Name="SatCountLimitComboBoxGhost" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0" Visibility="Hidden" SelectedItem="{x:Static satgen:SatCountLimitMode.Constellation}" MinWidth="{Binding ElementName=AutomaticLimitStackPanel, Path=ActualWidth}" ItemsSource="{Binding AvailableSatCountLimitModes}" />
          <StackPanel Name="AutomaticLimitStackPanel" Grid.Column="0" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="8,0">
            <ctr:NumericSpinner Name="AutomaticLimitNumericSpinner" MinWidth="50" Minimum="0" Maximum="999" Value="{Binding AutomaticSatCountLimit}" IsEnabled="{Binding SatCountLimitMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Automatic}}" />
            <Button Name="ResetButton" Content="Reset" Height="23" MinWidth="55" HorizontalAlignment="Center" Margin="8,0,0,0" Command="{Binding ResetSatCountLimit}" IsEnabled="{Binding SatCountLimitMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Automatic}}" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" IsEnabled="{Binding SatCountLimitMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Constellation}}">
            <Label Name="GpsLimitLabel" Content="GPS" Margin="0" Padding="0" VerticalAlignment="Center" IsEnabled="{Binding IsGpsPresent}" MinWidth="{Binding ElementName=GalileoLimitLabel, Path=ActualWidth}" />
            <ctr:NumericSpinner Name="GpsLimitNumericSpinner" Minimum="0" Maximum="37" Width="44" Margin="3,0,8,0" Value="{Binding GpsSatCountLimit}" IsEnabled="{Binding IsGpsPresent}" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="3" IsEnabled="{Binding SatCountLimitMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Constellation}}">
            <Label Name="GalileoLimitLabel" Content="GAL" Margin="0" Padding="0" VerticalAlignment="Center" IsEnabled="{Binding IsGalileoPresent}" MinWidth="{Binding ElementName=GpsLimitLabel, Path=ActualWidth}" />
            <ctr:NumericSpinner Name="GalileoLimitNumericSpinner" Minimum="0" Maximum="37" Width="44" Margin="3,0,8,0" Value="{Binding GalileoSatCountLimit}" IsEnabled="{Binding IsGalileoPresent}" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="1" IsEnabled="{Binding SatCountLimitMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Constellation}}">
            <Label Name="GlonassLimitLabel" Content="GLO" Margin="0" Padding="0" VerticalAlignment="Center" IsEnabled="{Binding IsGlonassPresent}" MinWidth="{Binding ElementName=BeiDouLimitLabel, Path=ActualWidth}" />
            <ctr:NumericSpinner Name="GloLimitNumericSpinner" Minimum="0" Maximum="37" Width="44" Margin="3,0,8,0" Value="{Binding GlonassSatCountLimit}" IsEnabled="{Binding IsGlonassPresent}" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="3" IsEnabled="{Binding SatCountLimitMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Constellation}}">
            <Label Name="BeiDouLimitLabel" Content="BDS" Margin="0" Padding="0" VerticalAlignment="Center" IsEnabled="{Binding IsBeiDouPresent}" MinWidth="{Binding ElementName=GlonassLimitLabel, Path=ActualWidth}" />
            <ctr:NumericSpinner Name="BeiDouLimitNumericSpinner" Minimum="0" Maximum="37" Width="44" Margin="3,0,8,0" Value="{Binding BeiDouSatCountLimit}" IsEnabled="{Binding IsBeiDouPresent}" />
          </StackPanel>
        </Grid>
      </GroupBox>
      <Button Name="CancelButton" Content="Stop" Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Center" MinWidth="75" Height="23" Margin="8,0,16,0" Click="OnCancelButtonClick" />
    </Grid>
    <TabControl Grid.Row="2" Padding="0" SelectedIndex="0" ItemsSource="{Binding VisibleSatellites}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal">
            <CheckBox Margin="0,1,0,0" IsChecked="{Binding IsEnabled}" />
            <TextBlock Margin="6,0,4,0" Text="{Binding ConstellationAndSatCountText}" />
          </StackPanel>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
      <TabControl.ContentTemplate>
        <DataTemplate>
          <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Hidden" IsEnabled="{Binding IsEnabled}" FocusVisualStyle="{x:Null}">
            <FrameworkElement.Resources>
              <ResourceDictionary>
                <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
                  <Setter Property="Height" Value="14" />
                </Style>
              </ResourceDictionary>
            </FrameworkElement.Resources>
            <FrameworkElement.Style>
              <Style TargetType="{x:Type ScrollViewer}">
                <Style.Triggers>
                  <DataTrigger Value="False" Binding="{Binding IsEnabled}">
                    <Setter Property="UIElement.Opacity" Value="0.6" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
            <ItemsControl ItemsSource="{Binding Satellites}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Orientation="Horizontal" Margin="2" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="Auto" />
                      <RowDefinition Height="*" />
                      <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <FrameworkElement.Style>
                      <Style TargetType="{x:Type Grid}">
                        <Style.Triggers>
                          <DataTrigger Value="False" Binding="{Binding IsEnabled}">
                            <Setter Property="UIElement.Opacity" Value="0.8" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </FrameworkElement.Style>
                    <i:Interaction.Triggers>
                      <si:DataEventTrigger si:DataEventTrigger.EventName="FlashHide">
                        <im:ControlStoryboardAction im:StoryboardAction.Storyboard="{StaticResource HideStoryboard}" im:ControlStoryboardAction.ControlStoryboardOption="Play" />
                      </si:DataEventTrigger>
                    </i:Interaction.Triggers>
                    <Label Grid.Row="0" HorizontalAlignment="Center" Padding="0" Margin="0,3" Content="{Binding Id}" IsEnabled="{Binding IsEnabled}" />
                    <CheckBox Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,4" IsChecked="{Binding IsEnabled}" Visibility="{Binding ElementName=root, Path=DataContext.SatCountLimitMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static satgen:SatCountLimitMode.Manual}}" />
                    <Slider Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" TickPlacement="Both" TickFrequency="10" LargeChange="10" SmallChange="1" Minimum="{Binding Group.AttenuationRange.Min, Mode=OneTime}" Maximum="{Binding Group.AttenuationRange.Max, Mode=OneTime}" IsEnabled="{Binding IsEnabled}" MouseDoubleClick="OnSliderDoubleClick">
                      <RangeBase.Value>
                        <MultiBinding Converter="{StaticResource AttenuationMultiValueConverter}">
                          <Binding Path="Attenuation" />
                          <Binding Path="Group.AttenuationRange.Min" Mode="OneTime" />
                        </MultiBinding>
                      </RangeBase.Value>
                    </Slider>
                    <TextBox Name="LevelTextBox" Grid.Row="3" IsReadOnly="True" HorizontalContentAlignment="Right" Width="44" Margin="2,4,2,-1" Text="{Binding Path=AttenuationText, Mode=OneWay}" IsEnabled="{Binding IsEnabled}" />
                    <Grid Name="Overlay" Row="0" RowSpan="4" Background="#FFFFA500" Opacity="0.0" IsHitTestVisible="False" HorizontalAlignment="Center" Width="{Binding ElementName=LevelTextBox, Path=ActualWidth}">
                      <i:Interaction.Triggers>
                        <si:DataEventTrigger si:DataEventTrigger.EventName="FlashShow">
                          <im:ControlStoryboardAction im:StoryboardAction.Storyboard="{StaticResource ShowStoryboard}" im:ControlStoryboardAction.ControlStoryboardOption="Play" />
                        </si:DataEventTrigger>
                      </i:Interaction.Triggers>
                    </Grid>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </DataTemplate>
      </TabControl.ContentTemplate>
    </TabControl>
    <CheckBox Content="Link sliders" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8,2,14,2" IsChecked="{Binding AttenuationsLinked}" />
  </Grid>
</UserControl>